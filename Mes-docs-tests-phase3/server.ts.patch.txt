// ---------------------------------------------------------------------------
// INTÉGRATION Phase 3 — Swagger UI /api-docs
// À ajouter dans server.ts, après le montage des routes /api/v1 (ligne ~149)
// ---------------------------------------------------------------------------

  // Swagger UI (Phase 3) — sert /api-docs + /api-docs/openapi.yaml
  app.use(
    "/api-docs",
    (await import("./server/src/routes/swagger.js")).default,
  );

// ---------------------------------------------------------------------------
// NB : placer la spec openapi.yaml à la racine du repo (à côté de server.ts)
//      OU dans server/src/routes/ — la route recherche les deux emplacements.
// ---------------------------------------------------------------------------
